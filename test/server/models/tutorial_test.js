require(TEST_HELPER) // <--- This must be at the top of every test file.

var request = require('supertest')
var Tutorial = require(__client + '/models/tutorial.js');

describe("The Tutorial Client Model", function() {
	
});